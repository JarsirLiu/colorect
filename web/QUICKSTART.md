# 快速启动指南

## 📋 前置要求

确保你的环境已安装：
- Node.js >= 18.x
- npm >= 9.x

## 🚀 项目初始化步骤

### 1. 进入前端项目目录

```bash
cd frontend
```

### 2. 安装依赖

```bash
npm install
```

### 3. 启动开发服务器

```bash
npm run dev
```

开发服务器将在 `http://localhost:3000` 启动。

### 4. 访问应用

在浏览器中打开 `http://localhost:3000`，你将看到：
- 🏠 **首页** - 展示所有可用的 AI 工具
- ✂️ **智能抠图** - 完整的抠图工具流程

## 🎯 主要功能

### 1. 智能抠图工具 (`/cutout`)

完整的 AI 抠图流程：
1. **上传图片** - 支持拖拽或点击选择
2. **前端校验** - 自动验证文件格式和大小
3. **上传进度** - 实时显示上传进度
4. **AI 处理** - 显示处理状态
5. **结果展示** - 对比原始图片和抠图结果
6. **下载保存** - 一键下载抠图结果

### 2. 首页 (`/`)

- 工具卡片展示
- 渐变色设计
- 响应式布局

## 🔧 配置说明

### 环境变量

创建 `.env` 文件（已提供）：

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

### 代理配置

开发环境已配置代理，`/api` 请求会自动转发到后端服务。

## 📁 项目结构概览

```
frontend/
├── src/
│   ├── components/           # 通用组件
│   │   ├── Button/          # 按钮组件
│   │   └── Loading/         # 加载组件
│   ├── features/            # 业务功能模块
│   │   └── cutout/          # 智能抠图
│   │       ├── components/  # 业务组件
│   │       ├── api.ts       # API 接口
│   │       └── page.tsx     # 页面
│   ├── pages/               # 页面
│   │   └── Home.tsx         # 首页
│   ├── services/            # API 服务
│   │   └── http.ts          # HTTP 客户端
│   ├── store/               # 状态管理
│   │   └── cutoutStore.ts   # 抠图状态
│   ├── styles/              # 样式
│   │   └── index.css        # 全局样式
│   ├── utils/               # 工具函数
│   │   ├── cn.ts            # className 合并
│   │   └── file.ts          # 文件处理
│   ├── App.tsx              # 根组件
│   ├── main.tsx             # 入口文件
│   └── router.tsx           # 路由配置
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## 🛠 可用命令

```bash
# 开发
npm run dev

# 构建
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint

# 代码格式化
npm run format
```

## 🎨 技术特性

- ✅ **TypeScript** - 完整的类型安全
- ✅ **React 18** - 最新的 React 特性
- ✅ **Vite** - 极速的开发体验
- ✅ **Tailwind CSS** - 现代化的样式方案
- ✅ **React Router** - 客户端路由
- ✅ **Zustand** - 轻量级状态管理
- ✅ **React Query** - 服务器状态管理
- ✅ **Axios** - HTTP 请求封装

## 📝 开发注意事项

### 添加新功能模块

1. 在 `src/features/` 下创建新目录
2. 创建必要的文件：`page.tsx`, `api.ts`, `components/`
3. 在 `src/router.tsx` 中添加路由
4. 在首页添加入口链接

### 样式规范

- 优先使用 Tailwind CSS utility classes
- 避免内联样式
- 使用 `cn()` 函数合并 className

### API 对接

- 所有 API 接口定义在模块的 `api.ts` 中
- 使用 `src/services/http.ts` 封装的 axios 实例
- 使用 TypeScript 定义接口类型

## 🐛 常见问题

### 1. 依赖安装失败

尝试清除缓存：
```bash
rm -rf node_modules package-lock.json
npm install
```

### 2. 端口被占用

修改 `vite.config.ts` 中的端口号：
```ts
server: {
  port: 3001  // 改为其他端口
}
```

### 3. 后端 API 请求失败

检查：
- `.env` 文件中的 `VITE_API_BASE_URL` 是否正确
- 后端服务是否已启动
- 网络连接是否正常

## 📚 下一步

- [ ] 添加更多 AI 工具模块
- [ ] 实现用户认证
- [ ] 添加工作历史记录
- [ ] 优化移动端体验
- [ ] 添加暗色模式

## 📖 参考资料

- [React 文档](https://react.dev/)
- [Vite 文档](https://vitejs.dev/)
- [Tailwind CSS 文档](https://tailwindcss.com/)
- [前端开发规范文档](../前端开发规范.md)
